


# We need to write configurations so that it can connect to the MySQL
# These are standard configurations to connect with DB

# Mandatory step : Always create the database

# Write these properties as it is to make the connection with MySQL.

spring.datasource.url=jdbc:mysql://localhost:3306/library_system?createTableIfNotExists=true
spring.datasource.username=root
spring.datasource.password=swati@123

# We are writing this for hibernate
spring.jpa.hibernate.ddl-auto=update
# It means if the Entity has been changed :---> update the SQL table also

server.port=10000
